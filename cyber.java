import java.util.*;
class cyber
{
    int gcd(int x,int y)
    {
        if(y==0)
        return x;
        else
        return gcd(y,x%y);
    }
    public static void main()
    {
        Scanner in=new Scanner(System.in);
        
        c=(a+b)%(b+gcd(a,b));
        if(c==0)
        
    }
}